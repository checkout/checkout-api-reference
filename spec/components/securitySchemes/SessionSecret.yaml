description: >
      A 26 character alpha-numeric value that gives access to a single authentication within the Sessions API. This value is returned as the `session_secret` when requesting a session. 
name: Authorization
type: apiKey
in: Header